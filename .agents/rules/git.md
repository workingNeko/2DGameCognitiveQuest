# Git Rules

- **DO NOT auto push**: Never run `git push` automatically or proactively.
- Only stage/commit locally or leave changes in working tree.
- Push to remote repository ONLY when the user explicitly requests a push.
